services:
  - type: web
    name: book-recommender-llm
    runtime: python
    buildCommand: ""
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=$PORT

    envVars:
      - key: PORT
        value: 10000
