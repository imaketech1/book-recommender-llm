services:
  - type: web
    name: book-recommender-llm
    runtime: python
    branch: deploy 
    buildCommand: ""
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=$PORT --workers 1
    envVars:
      - key: PORT
        value: 10000
